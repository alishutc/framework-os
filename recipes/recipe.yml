---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: framework-os
# description will be included in the image's metadata
description: Custom Fedora Silverblue OS for Framework Laptop 13 (AMD Edition).

# the base image to build on top of (FROM) and the version tag to use
base-image: quay.io/fedora-ostree-desktops/silverblue
image-version: 44

labels:
  containers.bootc.rootfs: btrfs

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* into image root /

  - type: dnf
    repos:
      nonfree: rpmfusion
    install:
      packages:
        - steam
        - steam-devices
        - powertop
        - mangohud

  - type: default-flatpaks
    configurations:
      - scope: user # Enable Flathub user repository for on-demand apps

  - type: gnome-extensions
    install:
      - Logo Menu
      - AppIndicator and KStatusNotifierItem Support
      - Dash to Dock
      - Blur my Shell
      - Search Light

  - type: gschema-overrides
    include:
      - 01-framework.gschema.override

  - type: signing # sets up proper policy & signing files for cosign verification
